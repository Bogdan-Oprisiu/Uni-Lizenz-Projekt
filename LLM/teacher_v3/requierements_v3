# Core ML/NLP Libraries
# Ensure torch version matches your CUDA version if using GPU
# Example for CUDA 11.7:
# torch==2.0.1+cu117 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117
# Example for CPU or other CUDA:
torch>=2.0.0
# --- PINNED TRANSFORMERS VERSION ---
transformers==4.38.2
# ---------------------------------
datasets>=2.10.0
tokenizers>=0.13.0 # Ensure tokenizer version compatibility if issues arise
sentencepiece

# --- PINNED PEFT VERSION ---
peft==0.8.2 # Pinning to a specific stable version
# ---------------------------

# Hugging Face Hub & Acceleration
huggingface-hub>=0.15.0
accelerate>=0.20.0 # Ensure accelerate is compatible

# Utilities
tqdm

# Optional: For 8-bit quantization of the base model
# bitsandbytes>=0.41.0

# Optional: If using pre-processing steps from previous pipeline
# pyspellchecker
